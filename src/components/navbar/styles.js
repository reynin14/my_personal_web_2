const container = {
  display: 'flex',
  margin: '20px',
  position: 'sticky',
  top: 0
}

const navButton = {
  margin: '20px'
}

const styles = {
  container,
  navButton
};

export default styles;
