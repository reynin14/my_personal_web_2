const container = {
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  backgroundColor: 'yellow',
};

const header = {
  fontSize: '4vh',
  top: '1vh',
  fontFamily: 'Raleway'
};

const underline = {
  fontSize: '3vh'
}

const styles = {
  container,
  header,
  underline,
};

export default styles;
