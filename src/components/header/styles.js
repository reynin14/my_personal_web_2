const container = {
  display: 'flex',
  flexDirection: 'column',
};

const header = {
  fontSize: '4vh',
  top: '1vh'
};

const underline = {
  fontSize: '3vh'
}

const styles = {
  container,
  header,
  underline,
};

export default styles;
