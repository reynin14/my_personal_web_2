const container = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  backgroundColor: '#252525',
}

const text = {
  font: 'Raleway',
  color: 'white',
  fontSize: '12pt',
  margin: '40px',
}

const subText = {
  color: 'red',
}

const styles = {
  container,
  text,
  subText,
};

export default styles;
