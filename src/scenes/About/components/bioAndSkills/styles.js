const container = {
  flex: 1,
}

const bioContainer = {
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
}

const hexagonContainer = {
  width: '300px',
  height: '300px',
  marginTop: '50px',
}

const hexagon = {
  stroke: '#000000',
}

const caption = {
  margin: '5px',
  font: 'Raleway',
  fontSize: '18pt',
  fontWeight: '900',
}

const description = {
  margin: '13px',
  width: '350px',
  textAlign: 'center',
}

const styles = {
  container,
  bioContainer,
  hexagonContainer,
  hexagon,
  caption,
  description,
};

export default styles;
