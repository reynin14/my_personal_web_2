const container = {
  height: '1200px',
  width: '100vw',
  backgroundColor: '#e0ae15',
  display: 'flex',
}

const infoContainer = {
  display: 'flex',
  width: '85vh',
  justifyContent: 'center',
}

const bio = {
  display: 'flex',
  textAlign: 'justify',
  margin: '10px'
}

const styles = {
  container,
  infoContainer,
  bio,
};

export default styles;
